[package]
name = "ynwa-repository"
version.workspace = true
edition.workspace = true
authors.workspace = true

[dependencies]
ynwa-core = { path = "../ynwa-core" }
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
tempfile = "3"

[lib]
path = "src/lib.rs"
