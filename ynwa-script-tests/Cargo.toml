[package]
name = "ynwa-script-tests"
version.workspace = true
edition.workspace = true
authors.workspace = true

[dependencies]
ynwa-core = { path = "../ynwa-core" }
serde_json = "1.0"
uom = "0.36"

[lib]
# This is a test-only crate, but we need lib.rs for integration tests
path = "src/lib.rs"
