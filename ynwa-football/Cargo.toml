[package]
name = "ynwa-football"
version.workspace = true
edition.workspace = true

[dependencies]
ynwa-core = { path = "../ynwa-core" }
ynwa-repository = { path = "../ynwa-repository" }
uom = { version = "0.36", features = ["f32"] }

[lib]
