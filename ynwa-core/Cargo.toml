[package]
name = "ynwa-core"
version.workspace = true
edition.workspace = true

[dependencies]
uom = { version = "0.36", features = ["f32"] }
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
rand = "0.9.2"

[lib]
